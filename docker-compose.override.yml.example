# Docker Compose Override Example
# Copy this file to docker-compose.override.yml and customize
#
# docker-compose.override.yml is automatically loaded and merged with docker-compose.yml
# This allows you to customize your local setup without modifying the main compose file
#
# Usage:
#   cp docker-compose.override.yml.example docker-compose.override.yml
#   # Edit docker-compose.override.yml with your customizations
#   docker-compose up

version: '3.8'

services:
  # ==========================================================================
  # Example: Custom API Configuration
  # ==========================================================================
  # api:
  #   # Use different ports
  #   ports:
  #     - "5002:5001"
  #   
  #   # Add extra environment variables
  #   environment:
  #     - CUSTOM_VAR=value
  #   
  #   # Mount additional volumes
  #   volumes:
  #     - ./custom-data:/app/custom-data
  #   
  #   # Increase resources
  #   deploy:
  #     resources:
  #       limits:
  #         cpus: '2'
  #         memory: 2G

  # ==========================================================================
  # Example: Custom Frontend Configuration
  # ==========================================================================
  # frontend:
  #   # Use different port
  #   ports:
  #     - "8080:80"
  #   
  #   # Custom build args
  #   build:
  #     args:
  #       - VITE_API_URL=http://custom-api:5001

  # ==========================================================================
  # Example: Enable PostgreSQL
  # ==========================================================================
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: dynoai-postgres
  #   restart: unless-stopped
  #   environment:
  #     - POSTGRES_USER=dynoai
  #     - POSTGRES_PASSWORD=your_secure_password_here
  #     - POSTGRES_DB=dynoai
  #   volumes:
  #     - dynoai-postgres:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U dynoai"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - dynoai-network

  # ==========================================================================
  # Example: Add Prometheus for Monitoring
  # ==========================================================================
  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: dynoai-prometheus
  #   restart: unless-stopped
  #   volumes:
  #     - ./config/prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #     - dynoai-prometheus:/prometheus
  #   ports:
  #     - "9090:9090"
  #   networks:
  #     - dynoai-network

  # ==========================================================================
  # Example: Add Grafana for Dashboards
  # ==========================================================================
  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: dynoai-grafana
  #   restart: unless-stopped
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=admin
  #   volumes:
  #     - dynoai-grafana:/var/lib/grafana
  #   ports:
  #     - "3000:3000"
  #   networks:
  #     - dynoai-network

# =============================================================================
# Custom Volumes
# =============================================================================
# volumes:
#   dynoai-postgres:
#     name: dynoai-postgres
#   dynoai-prometheus:
#     name: dynoai-prometheus
#   dynoai-grafana:
#     name: dynoai-grafana
